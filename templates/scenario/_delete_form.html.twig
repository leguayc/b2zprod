<form method="post" action="{{ path('app_scenario_delete', {'id': scenario.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet élément ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ scenario.id) }}">
    <button class="btn">Supprimer</button>
</form>
