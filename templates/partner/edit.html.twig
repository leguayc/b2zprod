{% extends 'backoffice.html.twig' %}

{% block title %}Modifier les partenaires{% endblock %}

{% block page %}
    <h1>Modifier les partenaires</h1>

    {{ include('partner/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <div class="contain-button">
        <a href="{{ path('app_partner_index') }}">Retour</a>

        {{ include('partner/_delete_form.html.twig') }}
    </div>

{% endblock %}
